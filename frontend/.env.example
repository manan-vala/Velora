
# Server-only (web build / Vercel): used by the API routes to reach the Cloudflare Worker
WORKER_URL=https://oracle-a1-worker.manan-vala.workers.dev
WORKER_TOKEN=

# Only set for build:app: the API root of the deployed web app
# NEXT_PUBLIC_API_BASE_URL=https://<vercel-domain>/api
